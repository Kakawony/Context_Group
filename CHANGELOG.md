# CHANGELOG — Context Group

## 2026-09-04 — Домен выбран: `ai-lapin.com`; почта убрана с сайта полностью
**Что делали:** Витал выбрал домен **`ai-lapin.com`** (дефис принят осознанно: у старого `context-group.ru` он тоже был, а связка «маркетинг + ИИ» — это ниша навсегда, не хайп-волна). Оба кандидата (`ai-lapin.com`, `lapinvital.com`) проверены: RDAP 404 = свободны, whois «No match» = записей о прежней регистрации нет, Internet Archive = НОЛЬ снимков за всю историю → домены девственные, наследства фильтров/спам-истории/чужих ссылок нет.
- `SITE_URL` в `astro.config.mjs` → `https://ai-lapin.com` (одна строка, как и задумано).
- **Почты на сайте больше нет вообще** (решение Витала): переписок по email не будет, все заявки падают в Telegram через `/send`. `CONTACT_EMAIL` удалён из `src/data/site.ts`, вместо него `CONTACT_TELEGRAM_URL`/`CONTACT_TELEGRAM_HANDLE`. В футере `Layout.astro` строка с mailto заменена на ссылку `@mrlapin`; в `privacy.astro` реквизит «Email» заменён на «Связь: Telegram @mrlapin» (способ связи в политике нужен, но теперь не почтовый). Следствие: **Cloudflare Email Routing из Фазы 1 отменяется**, почтовые записи новому домену не нужны.
- `package.json` `name` → `ai-lapin`: это же имя получает воркер в Cloudflare (адаптер берёт его в `dist/server/wrangler.json`), чтобы проект в дашборде совпадал с брендом.

**Проверка (независимая):** прод-билд зелёный; sitemap 27 URL на `https://ai-lapin.com/`; canonical главной на новом домене; `robots.txt` с новым Sitemap и Disallow'ами; имя воркера `ai-lapin`; `mailto`/`info@` в `dist/client` НЕ найдены ни одного; в футере отдаётся `@mrlapin`; упоминания почты в контенте Keystatic остались только там, где это описание услуги парсинга (сбор почт клиентов) — контактом не являются, не трогали.
**Почему:** домен куплен/выбран, дальше вся Фаза 1 идёт под него; почта — лишняя точка входа и лишняя настройка DNS, раз общение только в Telegram.
**К чему привело:** Код полностью готов под `ai-lapin.com`. Дальше Фаза 1: пуш в GitHub → Cloudflare Workers Builds → GitHub App для Keystatic → домен + ECH off → 301 с `context-group.ru` в `.htaccess` на Timeweb (1–2 месяца, потом бросить).

## 2026-09-04 — CLAUDE.md → указатель на правила хаба
**Что делали:** `CLAUDE.md` (65 строк) сокращён до 2 строк — импорт `Projects/context_group_hub/RULES.md`; содержимое перенесено туда целиком.
**Почему:** реструктуризация правил 04.09.2026.
**К чему привело:** правила проекта в одном месте; при первом запуске Claude Code спросит разрешение на внешний импорт — подтвердить.

## 2026-09-03 — Фаза 0 переезда: домен `lapinvital.com`, Cloudflare Workers, секреты без фолбэков, Keystatic 6
**Что делали:** По плану `context_group_hub/Docs/PLAN_migration_com_2026-09-03.md` (согласован с Виталом: личный бренд вместо агентства, переезд 1:1 → тексты → iGaming-раздел → EN позже) подготовили код к первому пушу в публичный репо и деплою на Cloudflare.
- **Домен — единая константа.** `context-group.com` (прошитый 19.08) оказался ЗАНЯТ третьим лицом. Новый домен `lapinvital.com` задан ОДИН раз — `SITE_URL` в `astro.config.mjs`; `robots.txt` стал роутом `src/pages/robots.txt.ts` (от `Astro.site`, + `Disallow: /keystatic`, `/api/`, `/send`, `/thanks`); 8 hidden `_next` во всех формах → относительный `/thanks`; `privacy.astro` берёт домен из `Astro.site.host`, адрес приведён к футеру (Таиланд, Районг). Email — один раз в `src/data/site.ts` (`CONTACT_EMAIL = info@lapinvital.com`), используется в футере `Layout.astro` и в политике. `grep context-group src public` → только `repo:` Keystatic.
- **`send.ts` без секретов в коде.** Убраны fallback-токен и chat_id (были в 3 незапушенных коммитах). **Находка:** в Astro 6 + `@astrojs/cloudflare@13` геттер `locals.runtime.env` УДАЛЁН и бросает исключение — старый код в проде молча сидел бы на фолбэк-токене. Переведено на `getSecret` из `astro:env/server` (адаптер: `envGetSecret: stable`). `_next` принимается только как относительный путь (защита от открытого редиректа).
- **Keystatic 5.2.0 → 6.0.0** (`@keystatic/core` 0.6.5 → 0.6.9): 5.2 читал `locals.runtime.env` и на воркере отдавал 500 на ВСЕ `/api/keystatic/*` (GitHub-режим на проде не работал бы). В 6.0.0 — `getSecret`.
- **Это Cloudflare Workers, а не Pages:** адаптер v13 собирает `dist/server/entry.mjs` + `dist/server/wrangler.json` (assets `../client`, KV `SESSION`), Pages из адаптера убран. Деплой — Workers Builds (Git). `public/.htaccess` (Timeweb, уезжал в `dist/client` и отдавался файлом) удалён; добавлен `public/_headers` (security-заголовки по образцу `site_diego`, без CSP); `Layout.astro` получил проп `lang` (дефолт `ru`) и `og:locale` — задел под `/en/`; удалён неиспользуемый импорт `ToolsGrid` и незамонтированная заглушка `ArticleLeadForm.astro`; `@markdoc/markdoc` и `wrangler` объявлены в `package.json` явно; скрипт `npm run deploy`; `.dev.vars` в `.gitignore`; README вместо стартер-болванки.
- **Грабли:** (1) `wrangler.jsonc` в корне репо ЛОМАЕТ сборку — vite-plugin Cloudflare переключается на свой workerd-раннер и падает на `virtual:keystatic-config`; файл убран, конфиг воркера генерит адаптер. (2) «The file does not exist at …/.vite/deps_ssr/…» — лечится `rm -rf node_modules/.vite .astro`. (3) `astro preview` крутит воркер в Node, а не в workerd — реальный тест: `cd dist/server && npx wrangler dev --config wrangler.json --local` (+ `.dev.vars` с тестовыми секретами).

**Проверка (независимая):** прод-билд зелёный в обоих режимах Keystatic (local и `PUBLIC_KEYSTATIC_MODE=github`); sitemap 27 URL на `lapinvital.com`; `robots.txt`, `_headers`, `lang="ru"`, `og:locale=ru_RU`, email в `dist` верные; `.htaccess` в `dist` нет. Под workerd (`wrangler dev --local`): `/` 200, `/ai-solutions` 307→`/ai-solutions/` 200, `/nonexistent` 404, `/keystatic` 200, `/send` GET 404; honeypot-POST → `{"ok":true}`; POST без секретов → 400 `{"ok":false}` (фолбэка нет); POST с тестовыми секретами из `.dev.vars` → 200 (getSecret читает env воркера); форма-POST → 303 на `https://lapinvital.com/thanks`, внешний `_next` игнорируется; `/api/keystatic/github/login` → 307 на `github.com/login/oauth/authorize` с client_id из секрета (было 500). Реальная отправка в Telegram — после ротации токена, на проде.
**Почему:** переезд на .com/Cloudflare без утечки секретов и с рабочей веб-админкой; домен и email должны меняться одной правкой.
**К чему привело:** Код готов к пушу. **Перед пушем обязательно:** ротация токена @contextgr_bot в BotFather (старый лежит в истории незапушенных коммитов) + склейка 3 коммитов в один (`git reset --soft 7409adf`). Дальше — Фаза 1 по плану (покупка домена, Workers Builds, GitHub App, ECH off, Email Routing, .ru-зона в CF с 301).

## 2026-08-19 — Подготовка к переезду на Cloudflare Pages + домен .com (переезд-first)
**Что делали:** Начали техподготовку к переезду сайта на Cloudflare Pages и домен `context-group.com` (стратегия: сначала технический переезд с 301, индексация, потом правка контента).
- **Формы:** `public/send.php` (PHP-релей в Telegram) на Cloudflare Pages не исполняется, а сырой `.php` в `public/` отдавался бы статикой и светил токен бота. Переписал логику один-в-один в серверный Astro-эндпоинт `src/pages/send.ts` (`prerender=false`, `POST`), токен/chat_id — из env Cloudflare (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`) с фолбэком на текущие значения. Все формы (`action`/`fetch`) переведены с `/send.php` на `/send` (10 мест: index×2, ServicePage×2, AuditPage×2, PromptLayout form+fetch, обучение×2). `public/send.php` удалён. **Грабля:** с адаптером `@astrojs/cloudflare` Astro собирает `_worker.js`, и каталог `/functions` Cloudflare Pages ИГНОРИРУЕТ — поэтому форму сделал Astro-роутом, а не Pages Function.
- **Домен .ru → .com в коде** (17 вхождений): `site` в `astro.config.mjs`, `Sitemap:` в `robots.txt`, `_next` во всех формах (8), тексты в `privacy.astro`. **Email `info@context-group.ru` НЕ трогал** (в подвале `Layout.astro` и в политике) — это почтовый ящик, а не адрес сайта; зафиксировано в CLAUDE.md.
- **Гигиена:** 7 одноразовых скриптов раскаток Gemini убраны из корня репо в хаб (`context_group_hub/Parsers_and_Scripts/gemini_rollout_2026-08-17/`) — нарушение архитектуры (в `Github/` только код сайта).
- **Первый коммит:** вся миграция Keystatic (17–18.08) + эти правки закоммичены локально (2 дня работы висели без git). БЕЗ пуша.
- **Keystatic storage переключаемый:** `storage` в `keystatic.config.ts` теперь зависит от env `PUBLIC_KEYSTATIC_MODE` — без переменной локальный режим (dev, как было), `=github` → GitHub-режим (репо `Kakawony/Context_Group`) для веб-админки на проде. Переключение без правки кода. Сборка зелёная в обоих режимах.
- **Деплой-инструкция** (самодостаточная, пошагово): `context_group_hub/Docs/DEPLOY_cloudflare_com_2026-08-19.md` — Cloudflare Pages + GitHub-режим Keystatic + домен/301/вебмастера.

**Проверка (независимая):** прод-билд `NODE_ENV=production` зелёный (`Complete!`, 30 роутов); sitemap генерится на `context-group.com`; эндпоинт `/send` в dev отвечает корректно (honeypot→`ok:true` без отправки; мусор→400; не-ajax→303 на `context-group.com/thanks`); Astro CSRF (`checkOrigin`) отбивает POST без `Origin` — реальная браузерная форма шлёт `Origin`, отработает. `.ru` в коде остался только в 2 email. Реальная отправка в бот не тестилась (проверяет Витал живой заявкой).
**Почему:** переезд на .com с сохранением приёма заявок и без утечки токена.
**К чему привело:** Код готов к деплою на Cloudflare Pages под `.com`. Остаётся (действия Витала в аккаунтах): создать проект Cloudflare Pages + env-секреты бота; Keystatic GitHub-режим (GitHub App/OAuth); DNS `.com` + 301 со старого `.ru` + Я.Вебмастер «Переезд» / GSC «Change of address». Опц.: ротация токена бота (светился в открытом репо), B4 (DRY конфига). **SEO-риск:** смена зоны .ru→.com для РФ-гео = временная просадка в Яндексе (решение принято осознанно).

## 2026-08-17 — CMS Keystatic: приёмка коллекции «Промпты»
**Что делали:** Проверил добавление Gemini коллекции `prompts` в `keystatic.config.ts` по ТЗ `TZ_GEMINI_prompts_collection_2026-08-17.md` (по образцу `blog`). Промпты теперь добавляются/правятся в админке: раздел (`category` — `fields.select`, 8 разделов), теги (array), цены, 2 оффер-карточки (плоские поля offer1*/offer2*), тело в rich-markdoc. Рендер (`/prompts` листинг, `PromptLayout`, сам `.md`) НЕ трогали.
**Проверка (независимая):** коллекция `prompts` добавлена, `blog` не тронут; схема покрывает ВСЕ 18 полей frontmatter существующего промпта + тело (ничего не срежется при сохранении); `category`-select содержит все 8 разделов + «Недвижимость» (значение промпта); прод-билд зелёный; `/prompts`, страница промпта, `/keystatic/collection/prompts` — HTTP 200; листинг рендерится 1:1; в админке коллекция «Промпты (каталог)» рядом с «Блог», промпт открывается, раздел-select работает («Недвижимость» выбрана), теги/офферы/тело на месте, Add создаёт новый.
**Почему:** Витал будет пополнять каталог промптов с тегами и разделами.
**К чему привело:** Каталог промптов управляется из админки. Разделы: Недвижимость/SEO/Контент и тексты/Маркетинг и реклама/Продажи и лиды/Парсинг и данные/Автоматизация/Сайты и разработка (select, новые добавляет Claude). Осталось опц.: B4 (DRY конфига). НЕ коммитили. (Фильтр по разделам на /prompts — отдельная будущая фича.)

## 2026-08-17 — CMS Keystatic: приёмка B3-раскатка №3 (кастовые) — B3 ЗАКРЫТ
**Что делали:** Финальная раскатка rich-SEO на 2 кастовые страницы (`ai-solutions` — seoParagraphs; `ai-real-estate` — seoIntro+seoBlocks{title,html}, 6 блоков) по ТЗ `TZ_GEMINI_B3_rollout_custom_2026-08-17.md`. У обеих свой инлайновый рендер и стили — заменены на markdoc→`Fragment set:html`, SEO-стили переведены в `:global`, **добавлен `:global(a)` (cyan+underline)** — без него ссылки из rich-редактора были бы невидимы (доктрина). `.mdoc` сгенерил скриптом (мультиабзац в блоке «Преимущества…» разбит корректно). `contacts` НЕ трогали — у неё нет SEO-блока (только мини-био aboutHtml; Витал решил оставить как есть).
**Проверка (независимая):** 2 `.mdoc` байт-в-байт; старые SEO-поля вычищены из 2 JSON; конфиг `seoRich`=10 (ВСЕ SEO-страницы); `:global(a)` в обеих; createReader нет; прод-билд Cloudflare зелёный; HTTP 200; рендер ai-real-estate 1:1 (6 H3 с бирюзовыми маркерами, мультиабзац на месте) — скрин.
**К чему привело:** **B3 полностью закрыт — rich-редактор SEO на всех 10 страницах с SEO-полотном** (5 услуг + 3 аудита + 2 кастовые). Вся текстовая правка сайта в админке идёт без HTML. Осталось опционально: `fields.image` (загрузка картинок), промпт-страницы в CMS, B4 (DRY конфига). НЕ коммитили.

## 2026-08-17 — CMS Keystatic: приёмка B3-раскатка №2 (rich-SEO на 3 аудита)
**Что делали:** Проверил раскатку Gemini по ТЗ `TZ_GEMINI_B3_rollout_audits_2026-08-17.md`. Разовая доработка `AuditPage.astro` (зеркало ServicePage: пропс `seoRichHtml` + `<Fragment>` + SEO-стили в `:global`) + rich-SEO на 3 аудита. `.mdoc` сгенерил скриптом `gen_seo_mdoc.py` заранее, вложил в ТЗ.
**Проверка (независимая):** 3 `.mdoc` байт-в-байт с эталоном; `seoIntro/seoBlocks` вычищены из 3 JSON; конфиг `seoRich`=8 (5 услуг + 3 аудита); AuditPage доработан корректно (частный случай `audit-vnedreniya-ai` без ctaTitle учтён); createReader нет; прод-билд Cloudflare зелёный; 3 аудита HTTP 200; рендер audit-vnedreniya-ai 1:1 (H3-маркеры + 2 ссылки бирюзовые) — скрин.
**Почему:** раскатка rich-SEO на аудиты.
**К чему привело:** 8 из 11 страниц (5 услуг + 3 аудита) на rich-SEO. Осталось 3 кастовые. **ВАЖНО:** при подготовке ТЗ №3 выяснилось, что реальность кастовых расходится с планом — у `contacts` НЕТ SEO-блока (только `profile.aboutHtml`), у ai-solutions `seoParagraphs[]` (без ссылок), у ai-real-estate `seoIntro`+`seoBlocks[{title,html}]` (6 блоков, есть мультиабзац). НЕ коммитили.

## 2026-08-17 — CMS Keystatic: приёмка B3-раскатка №1 (rich-SEO на 4 услуги)
**Что делали:** Проверил раскатку Gemini по ТЗ `TZ_GEMINI_B3_rollout_services_2026-08-17.md` — rich-редактор SEO на `sozdanie`, `rekonstrukciya`, `performance-marketing`, `parsing-dannyh` (та же механика, что пилот; `ServicePage` не менялся). `.mdoc`-контент я сгенерил заранее скриптом `context_group_hub/Parsers_and_Scripts/gen_seo_mdoc.py` (перенос seoIntro+seoBlocks с конвертацией `<a>`→markdown) и вложил в ТЗ, чтобы Gemini вставил дословно.
**Проверка (независимая):** 4 `.mdoc` **байт-в-байт** совпали с эталоном ТЗ (транскрипции нет); `seoIntro/seoBlocks` вычищены из 4 JSON; в конфиге `seoRich` теперь у 5 услуг (4 + пилот), старых полей у них нет; createReader нет; прод-билд Cloudflare зелёный; рендер parsing-dannyh 1:1 (5 H3 с маркерами, абзацы) — скрин.
**Почему:** раскатка главной фичи (rich SEO) на типовые услуги после принятого пилота.
**К чему привело:** У всех 5 услуг SEO-текст правится в визуальном редакторе. Осталось: 3 аудита (разовая правка AuditPage + страницы) и 3 кастовые (bespoke). НЕ коммитили.

## 2026-08-17 — CMS Keystatic: приёмка B3-ПИЛОТ (rich-редактор SEO на seo-konvejer)
**Что делали:** Проверил пилот Gemini по ТЗ `TZ_GEMINI_B3_pilot_richseo_2026-08-17.md` — перевод SEO-текста ОДНОЙ страницы `seo-konvejer` (вступление + 4 подраздела + inline-ссылка) с сырого HTML на визуальный rich-редактор Keystatic (`fields.document` → `seoRich.mdoc` → `@markdoc/markdoc` + `mdoc?raw`, БЕЗ createReader). В общий шаблон `ServicePage.astro` добавлен обратно-совместимый опциональный пропс `seoRichHtml` (условный `<Fragment set:html>`), SEO-стили узлов h3/p/strong/ul/li переведены в `:global` (иначе rich без стилей).
**Проверка (независимая):** прод-билд Cloudflare зелёный (node:path/workerd не всплывали); createReader нигде; конфиг — `seoRich` только у seo-konvejer, у остальных 4 услуг `seoIntro/seoBlocks` не тронуты; рендер seo-konvejer 1:1 (H3 с бирюзовыми маркерами, ссылка «создание сайтов…» бирюзовая с подчёркиванием — скрин); **регрессия sozdanie 1:1** (общий шаблон не поехал от перевода стилей в :global — скрин).
**Почему:** B3 — главная фича доводки (правка SEO без HTML в визуальном редакторе). Пилот на 1 странице перед раскаткой.
**К чему привело:** Механизм rich-SEO для ServicePage-страниц доказан и безопасен. Дальше — раскатка на остальные 10 (4 услуги — тривиально, шаблон готов; 3 аудита — та же разовая правка AuditPage + 3 страницы; 3 кастовые — bespoke). НЕ коммитили.

## 2026-08-17 — CMS Keystatic: приёмка B2 (разбивка градиент-заголовков)
**Что делали:** Проверил работу Gemini по ТЗ `TZ_GEMINI_B2_split_headings_2026-08-17.md` — разбивка всех акцентных заголовков на поля `...Lead`/`...Accent` (правка без HTML) на 11 страницах.
**Проверка (независимая):** прод-билд зелёный; в 11 JSON не осталось старых полей (`heroTitle/processTitle/ctaTitle/blogTitle`) и `gradient-text` — всё разбито; счётчики Lead/Accent точные (услуги 4, аудиты с cta 6, ai-real-estate 4, contacts agents.title split, perf-marketing 5 с `heroTitleTail`); в конфиге `heroTitleLead×12 / processTitleLead×8 / ctaTitleLead×2 / heroTitleTail×1` — сходится; шаблоны `ServicePage`/`AuditPage` НЕ тронуты (`set:html={heroTitle}` на месте, заголовок собирается в frontmatter страницы и передаётся пропом). Рендер 1:1 глазами: perf-marketing (особый случай — акцент в середине + белый хвост « с AI»), seo-konvejer, ai-solutions.
**Почему:** B2 из роадмапа доводки админки — чтобы Витал правил заголовки без HTML.
**К чему привело:** Заголовки на 11 страницах редактируются 2 полями (начало + выделенное). Билд зелёный, рендер 1:1. НЕ коммитили. Осталось: B3 (rich-текст SEO-блоков, пилотом), опц. `fields.image`, промпты, B4.

## 2026-08-17 — CMS Keystatic: приёмка B1 (иконки) + доводка блога Gemini
**Что делали:**
- **B1 принят.** Проверил вынос SVG-иконок из JSON: создан `src/data/icons.ts` (словарь SVG), в JSON слаги (`building`/`browser`/`tg`/`wa`), в админке поле иконки стало `select`. Рендер 1:1 (building/tg/wa — глазами), прод-билд зелёный, сырого SVG в контенте не осталось.
- **Блог (отдельная задача Gemini) — починил косяки:** (1) рассинхрон `import.meta.glob` — унифицировал все 4 файла (`index.astro`, `[category].astro`, `BlogGrid`, `RelatedArticles`) на `*.md` (было `{md,mdoc}` в двух — латентный баг); (2) `category` в blog-коллекции переведён с `fields.text` на `fields.select` (Кейсы/Индустрия/Карьера) — защита от опечаток; (3) одноразовый скрипт `add-preview.cjs` убран из корня репо в хаб (`context_group_hub/Parsers_and_Scripts/add-preview_gemini_2026-08-17.cjs`); (4) `.wrangler/` добавлен в `.gitignore`.
- **Проверил всю блог-миграцию:** blog-index singleton (тексты листинга /blog), blog-коллекция (7 статей с rich-markdoc редактором контента), previewUrl на всех сущностях. Схема Keystatic покрывает все 8 полей frontmatter статей (при сохранении ничего не срежется). Листинг /blog, /blog/cases, /blog/career, страница статьи — 200, рендер с картинками ок.
**Почему:** Gemini параллельно (пока Claude был в лимитах) по заказу Витала перенёс блог в CMS; нужно принять B1 и довести блог до продакшн-качества.
**К чему привело:** Весь сайт (13 страниц + блог-листинг + статьи) редактируется через `/keystatic`. Билд зелёный. НЕ коммитили. Осталось: B2 (разбивка градиент-заголовков), B3 (rich-текст SEO-блоков на 11 страницах), опц. `fields.image` для загрузки картинок, промпт-страницы. Отчёт: `context_group_hub/Docs/CLAUDE_REVIEW_report_2026-08-17.md`.

## 2026-08-17 — CMS Keystatic: проверка миграции Gemini (Фаза A, верификация)
**Что делали:** Перепроверили работу Gemini (11 страниц, перенесённых плоской моделью JSON) по ТЗ `context_group_hub/Docs/TZ_CLAUDE_review_keystatic_2026-08-17.md`. Прогнали чистый прод-билд (`rm -rf node_modules/.vite dist && NODE_ENV=production npm run build`) — зелёный. Сняли headless-скрины всех 11 страниц, глазами просмотрели 5 (3 кастовые + ServicePage + AuditPage), остальные 6 — структурная проверка рендера (H1/тарифы/FAQ/SEO). Открыли админку `/keystatic` для `sozdanie` и `contacts` — формы, русские подписи, массивы с itemLabel. Скриптом `context_group_hub/Parsers_and_Scripts/verify_text_fidelity.py` сверили весь текст с бэкапом `pre-gemini-2026-08-16`.
**Почему:** Перед доводкой (Фаза B) нужно поймать регрессии от миграции Gemini.
**К чему привело:** Регрессий НЕ найдено — билд зелёный, рендер 1:1, текст на месте, админка правится. Код НЕ менялся. Отчёт: `context_group_hub/Docs/CLAUDE_REVIEW_report_2026-08-17.md`. Дальше — Фаза B (B1 SVG→шаблон, B2 градиент-заголовки, B3 rich-текст SEO, B4 DRY) ждёт решений Витала. Ничего не коммитили.

## 2026-08-16 — CMS Keystatic: раскатка на ГЛАВНУЮ (Фаза 3, начало) + PoC rich-text
**Что делали:** Перевели всю **главную** (`src/pages/index.astro`) на Keystatic. Плюс освоили rich-text редактор (визуальный, как Word) — это была развилка, Витал выбрал его вместо markdown-полей.
- **PoC rich-text под Cloudflare (грабля №1).** Проверили гипотезу до раскатки: `fields.document` пишет контент в ОТДЕЛЬНЫЙ `.mdoc`-файл (не в `index.json`!), поэтому «прямой импорт JSON» его не отдаёт. Решение: читать `.mdoc` как raw-строку (`import x from '...mdoc?raw'` — Vite подставляет содержимое на билде, в Node) и рендерить через `@markdoc/markdoc` (`Markdoc.parse→transform→renderers.html`, чистый JS, живёт в workerd). НЕ `createReader` (он тянет `node:path` → падение под Cloudflare). Прогнали `NODE_ENV=production npm run build` на минимальном примере — зелёный, `node:path` не всплыл.
- **`keystatic.config.ts`:** новый singleton «Страница: Главная» (`glavnaya`, local storage). Поля сгруппированы через `fields.object` по блокам: Hero, Услуги (массив 6 карточек), Баннер обучения, Кейсы (массив), Отрасли (массив), SEO-таблицы (команда 7 строк + сравнение услуг 5 строк, как массивы), FAQ (массив), Форма контакта. 3 rich-блока SEO-текста — `fields.document`.
- **Сид:** `src/content/pages/glavnaya/index.json` (все плоские поля/массивы) + `src/content/pages/glavnaya/index/seoBlock1..3.mdoc` (rich-текст, тексты один-в-один со старой версткой).
- **`src/pages/index.astro`:** хардкод текста заменён на `{data.*}` (прямой импорт JSON), 3 SEO-блока — через `<Fragment set:html={renderMdoc(...)} />`. Скелет HTML сохранён 1:1 (в т.ч. пред-существующая несбалансированная вложенность секций industries/seo — намеренно НЕ трогал, на ней завязаны отступы). Meta title/description пока хардкод (как в пилоте).
- **`src/components/ServicesSection.astro`:** переведён на пропсы (`titleLead/titleAccent/items`), тексты 6 карточек — из CMS; SVG-иконки и цвета остались в компоненте (дизайн, сопоставление по индексу).
- **CSS-грабля:** rich-текст идёт через `set:html` → Astro не проставляет scope-атрибут на вставленные узлы. Селекторы `.seo-text-content h2/h3/p/a/ul/li/strong` переведены на `:global()` (таблицы рендерятся в шаблоне — остались scoped).

**Почему:** Прямое требование Витала — начинать раскатку CMS С ГЛАВНОЙ (на пилоте «Обучение» обожглись, что сделали не главную и не спросили). Rich-редактор — чтобы длинный SEO-текст со ссылками правился визуально.

**К чему привело:** `NODE_ENV=production npm run build` зелёный (Cloudflare-адаптер). Homepage рендерится 1:1 со старым дизайном (проверено headless-скрином: hero, 6 услуг с бейджем, кейс с VS, SEO-текст с маркерами h2/h3, обе таблицы, cyan inline-ссылки /prompts /blog/cases /audit, FAQ, форма). Админка `/keystatic/singleton/glavnaya` открывается, все поля с русскими подписями, rich-редактор подхватил сид. НЕ коммитили/пушили. Дальше — раскатка остальных страниц (черновик делегируется Gemini по ТЗ `Projects/context_group_hub/Docs/`, Claude потом правит).

## 2026-08-16 — CMS Keystatic: пилот на странице обучения (Фаза 1)
**Что делали:** Подключили headless-CMS **Keystatic** (git-based) к Astro как пилот, чтобы тексты правились из админки без кода. Целевой хостинг — Cloudflare Pages.
- Установлены: `@keystatic/core`, `@keystatic/astro`, `@astrojs/react` (react@6.0.2), `@astrojs/cloudflare@13` (v14 требует Astro 7, у нас 6 — взяли v13).
- `astro.config.mjs`: интеграции `react()` + `keystatic()`; Cloudflare-адаптер подключается **условно только в прод-сборке** (`NODE_ENV==='production'`). Причина: в dev админка Keystatic должна работать в Node — под workerd-runtime Cloudflare падает `module is not defined` / `node:path`.
- `keystatic.config.ts`: singleton «Страница: Обучение» (local storage), все тексты + массив уроков вынесены в поля с русскими подписями.
- Контент-сид: `src/content/pages/obuchenie/index.json` (текущие тексты).
- `src/pages/obuchenie-marketingu-s-ai/index.astro`: тексты/уроки теперь читаются **прямым импортом JSON** (`import edu from '.../index.json'`), а НЕ через `createReader` — reader лезет в `node:fs/path` и падает при prerender под Cloudflare workerd. JSON-импорт работает в любом рантайме, Keystatic правит тот же файл.

**Почему:** Витал хочет сам править каждый текст/превью в админке; тексты сейчас «иишные», переписывать в чате неудобно. Пилот на 1 странице — оценить удобство до раскатки на весь сайт.

**К чему привело:** `npm run build` зелёный (Cloudflare-адаптер, статика + on-demand роуты админки). Локально `npm run dev` → админка `/keystatic` открывается (singleton «Страница: Обучение», поля редактируются, есть массив уроков), публичная страница рендерится из JSON без визуальных изменений. НЕ коммитили/пушили. Дальше: Фаза 2 (медиа-модель с переиспользуемыми превью), Фаза 3 (раскатка на все страницы), Фаза 4 (GitHub-режим админки + автодеплой Cloudflare Pages + домен .com). 4 npm-уязвимости в dev-зависимостях Keystatic — разобрать позже.

## 2026-08-16 — Раздел «Обучение маркетингу в паре с AI» (2 новые страницы + блок на главной + шапка)
**Что делали:**
- Новая страница `src/pages/obuchenie-marketingu-s-ai/index.astro` (URL `/obuchenie-marketingu-s-ai`): hero-интро → программа из 8 уроков (название + описание + итог; урок 1 кликабелен, остальные с меткой «Скоро») → блок «Обо мне» (аватар-инициалы ВЛ, факты) → CTA «Первый урок бесплатно» → раздел блога категории «Карьера» (тянет статьи через `import.meta.glob('../blog/*.md')`, фильтр по категории) → общий футер.
- Новая страница `src/pages/obuchenie-marketingu-s-ai/urok-1-analiz-konkurentov.astro`: заголовок + описание + окно видео 16:9 (**плейсхолдер-заглушка** с pulse-кнопкой Play, сюда позже встанет embed).
- Блок «Обучение маркетингу в паре с AI» на главной (`src/pages/index.astro`) — вставлен между секциями «Кейсы» и «AI-инструменты по отраслям»: бейдж «Новое · Обучение», текст, кнопка «Перейти к обучению» + ссылка на первый урок.
- Шапка (`src/layouts/Layout.astro`): пункт «Обучение маркетингу с AI» добавлен в конец дропдауна «Услуги» (десктоп + мобайл) акцентным цветом `--accent-purple`. Поднял `max-height` мобильного аккордеона услуг 100px → 360px (иначе 7-й пункт обрезался бы).

**Почему:** Витал попросил добавить направление обучения. CTA первого урока по согласованию ведёт сразу на страницу с видео (без формы-захвата); видео — плейсхолдер до записи; выделение пункта в меню — только цветом; слаг длинный SEO-вида для консистентности с остальными услугами.

**К чему привело:** `npm run build` зелёный, 29 страниц (обе новые собраны). Проверено рендером headless-Chrome + встроенным браузером (дропдаун с акцентным пунктом). Тексты уроков и «Обо мне» — черновые заготовки, дорабатываем позже. В прод не выгружалось.

**Правки по фидбэку (тот же день):** убрал вторую кнопку «Программа обучения» из hero (программа сразу ниже); добавил 9-й урок «Аналитика, отчёты и масштабирование» — сетка стала ровной 3×3; поставил реальное фото `public/images/vitaly-lapin.jpg` вместо инициалов в блоке «Кто ведёт обучение» (object-fit cover, рамка); убрал ссылку в блог из описания «Обо мне» (карьерный блок и так ниже); выровнял ширину CTA-карточки «Первый урок бесплатно» с блоком «Обо мне» (820→980px); карьерный блог теперь показывает все 5 статей категории «Карьера» (снял slice(0,3)); счётчики уроков синхронизированы (9 уроков / «ещё 8»).

**Правки по фидбэку №2 (тот же день):**
- Главная: блок обучения перенесён из позиции «после кейсов» на позицию **сразу после `<ServicesSection />`** (до кейсов) и сделан компактным баннером — убрал огромный padding (44px→28px) и glow, укоротил текст до 3 строк, `grid 1fr auto` (текст + столбик кнопка/ссылка). Больше не занимает пол-экрана.
- Страница обучения: самопальный блок «Обо мне» заменён на **переиспользование компонента `TeamSection.astro`** (единая стилистика сайта — карточка Имя/Роль/Телефон + фото + соцкнопки + сетка 6 AI-агентов), удалены все локальные `about-*` стили. В `TeamSection` добавлены опциональные пропсы `heading`/`subheading` (дефолты = прежний текст, обратно совместимо) — на обучении передан «Кто ведёт обучение».

**Правки по фидбэку №3 (монетизация обучения, тот же день):**
- Страница обучения: в hero добавлена стоимость «Полное обучение — 40 000 ₽ · можно разбить на 2–4 части» + вторая кнопка «Оставить заявку на обучение» (якорь `#zayavka`). На карточках уроков метка «Скоро» → «После оплаты» (акцентный фиолетовый). Карьерный блог: убрана кнопка «Все статьи о карьере», сетка переведена grid→flex `justify-content:center` — 5 статей теперь 3 сверху + 2 по центру снизу. Нижний CTA «Первый урок бесплатно» переделан в **форму-заявку** «Индивидуальное полное обучение» (info + цена 40 000 ₽/рассрочка слева, форма имя+контакт справа, `action=/send.php`, service-тег).
- Страница урока (`urok-1`): под видео добавлена **форма захвата «Получить всю программу»** (описание + цена 40 000 ₽/рассрочка + форма имя+контакт, `action=/send.php`). Блок «← Вся программа обучения» сохранён под формой.
- Цена 40 000 ₽ и рассрочка 2–4 части указаны в трёх точках: hero программы, нижняя форма-заявка, форма на странице урока.

## 2026-08-16 — Блок «Как проходит аудит»: с 3 до 6 шагов на трёх страницах
**Что делали:** В `processSteps` добавили по 3 карточки (04–06) на страницах `src/pages/audit-vnedreniya-ai.astro`, `audit-nedvizhimost.astro`, `audit-stomatologiya.astro`. Рендер идёт через общий `src/components/AuditPage.astro` (секция `.audit-steps`, сетка `repeat(3,1fr)`), правок компонента/CSS не потребовалось.
**Почему:** Витал попросил расширить блок до 6 пунктов; тексты 04–06 — черновые заготовки под нишу (согласование приоритетов → внедрение силами AI-роботов → замер и поддержка), дорабатываем позже.
**К чему привело:** Блок лёг ровно в 2 ряда по 3 карточки на всех трёх страницах (проверено рендером headless-Chrome на localhost:4321). В прод не выгружалось.
